export const serverResponseCode = {
  REST_INVALID_PARAM: "rest_invalid_param",
  REST_INVALID_DATE: "rest_invalid_date",
} as const;

export const wpAPIRequestParam = {
  AFTER: "after",
  BEFORE: "before",
} as const;
